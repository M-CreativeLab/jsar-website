# 运行项目

在上一章节中已经可以创建了一个项目，但是还没有运行起来，接下来我们就来运行一下项目。

![screenshot](howto-run-project.png)

- 首先，打开任何 `*.xsml` 文件
- 并选中打开的 Tab 页，在右上角可以找到打开场景视图的按钮
- 点击按钮，就可以打开场景视图

在打开场景视图后，有两个功能按钮：

- 重置位置，将场景视图的位置重置到原点
- 刷新，重新加载场景视图

你可以在需要时，点击这两个按钮。另外，当你通过编辑器修改了项目文件时，场景视图会自动刷新。
